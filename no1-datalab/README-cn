***********************
CS:APP 数据实验（Data Lab）
学生指南
***********************

你的目标是修改 bits.c 的副本，使其通过 btest 中的所有测试，同时不违反任何编码规范。


*********
0. 文件说明：
*********

Makefile	- 用于编译 btest、fshow 和 ishow
README		- 英文原版说明
README-cn	- 本文件（中文译本）
bits.c		- 你需要修改并提交的文件
bits.h		- 头文件
btest.c		- btest 主程序
  btest.h	- 用于编译 btest
  decl.c	- 用于编译 btest
  tests.c       - 用于编译 btest
  tests-header.c- 用于编译 btest
dlc*		- 规则检查编译器二进制文件（数据实验编译器）
driver.pl*	- 使用 btest 和 dlc 自动评分的驱动程序
Driverhdrs.pm   - 可选「Beat the Prof」竞赛的头文件
fshow.c		- 用于查看浮点数表示形式的工具
ishow.c		- 用于查看整数表示形式的工具

***********************************************************
1. 修改 bits.c 并使用 dlc 检查是否符合规范
***********************************************************

重要提示：开始之前请仔细阅读 bits.c 文件中的说明。其中给出了编码规则，只有遵守这些规则才能获得满分。

使用 dlc 编译器（./dlc）自动检查你的 bits.c 是否符合编码规范：

       unix> ./dlc bits.c

如果代码没有问题，dlc 会静默返回。
否则会打印消息，标出所有问题。使用 -e 选项运行 dlc：

    	unix> ./dlc -e bits.c

dlc 会打印每个函数使用的运算符数量。

得到符合规范的解法后，可以使用 ./btest 程序测试其正确性。

*********************
2. 使用 btest 进行测试
*********************

本目录中的 Makefile 会将你的 bits.c 与额外代码一起编译，生成名为 btest 的程序（测试框架）。

要编译并运行 btest 程序，请输入：

    unix> make btest
    unix> ./btest [可选命令行参数]

每次修改 bits.c 后都需要重新编译 btest。在不同平台之间切换时，应删除旧版 btest 并重新生成。使用以下命令：

    unix> make clean
    unix> make btest

btest 通过对每个函数运行数百万个测试用例来检验代码的正确性。它会广泛测试已知边界情况附近的输入，例如整数谜题的 Tmin 和零，以及浮点谜题的零、无穷大，以及非规格化数与规格化数之间的边界。当 btest 检测到某个函数出错时，会打印失败的测试、错误结果和期望结果，然后停止对该函数的测试。

btest 的命令行选项如下：

  unix> ./btest -h
  Usage: ./btest [-hg] [-r <n>] [-f <name> [-1|-2|-3 <val>]*] [-T <time limit>]
    -1 <val>  指定第一个函数参数
    -2 <val>  指定第二个函数参数
    -3 <val>  指定第三个函数参数
    -f <name> 仅测试指定名称的函数
    -g        以自动评分格式输出，不显示错误信息
    -h        打印此帮助信息
    -r <n>    为所有题目设置统一权重 n
    -T <lim>  将超时限制设为 lim

示例：

  测试所有函数的正确性并打印错误信息：
  unix> ./btest

  以紧凑形式测试所有函数，不显示错误信息：
  unix> ./btest -g

  测试函数 foo 的正确性：
  unix> ./btest -f foo

  使用指定参数测试函数 foo 的正确性：
  unix> ./btest -f foo -1 27 -2 0xf

btest 不会检查你的代码是否符合编码规范。请使用 dlc 进行该项检查。

*******************
3. 辅助程序
*******************

我们提供了 ishow 和 fshow 程序，分别用于解析整数和浮点数的表示形式。每个程序接受一个十进制或十六进制数作为参数。编译方法：

    unix> make

使用示例：

    unix> ./ishow 0x27
    Hex = 0x00000027,	Signed = 39,	Unsigned = 39

    unix> ./ishow 27
    Hex = 0x0000001b,	Signed = 27,	Unsigned = 27

    unix> ./fshow 0x15213243
    Floating point value 3.255334057e-26
    Bit Representation 0x15213243, sign = 0, exponent = 0x2a, fraction = 0x213243
    Normalized.  +1.2593463659 X 2^(-85)

    linux> ./fshow 15213243
    Floating point value 2.131829405e-38
    Bit Representation 0x00e822bb, sign = 0, exponent = 0x01, fraction = 0x6822bb
    Normalized.  +1.8135598898 X 2^(-126)


